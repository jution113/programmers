class Solution {
    public int solution(String message) {
        int answer = 0;
        final int width = 2; 
        int length = message.length();
        
        answer = width * length;
            
        return answer;
    }
}